# GENERATED BY: conduit template json_to_s3
# DESCRIPTION: Write JSON records to S3
# LAST UPDATED: 2025-01-15

version: "1.0"
project:
  name: "json_to_s3_pipeline"               # UPDATE THIS
  environment: "production"

sources:
  - type: "json"
    name: "json_source"
    config:
      path: "./data/input.json"             # UPDATE THIS

destinations:
  - type: "s3"
    name: "s3_dest"
    config:
      bucket: "YOUR_BUCKET"                 # UPDATE THIS
      prefix: "output/path/"                # UPDATE THIS
      aws_access_key_id: "${AWS_ACCESS_KEY_ID}"
      aws_secret_access_key: "${AWS_SECRET_ACCESS_KEY}"

pipelines:
  - name: "json_to_s3_pipeline"
    source: "json_source"
    destination: "s3_dest"
    mode: "append"

# HOW TO USE:
# 1. Update all fields.
# 2. Set required environment variables for credentials.
# 3. Save as `pipeline.yml`.
# 4. Run: `conduit run pipeline.yml`.
