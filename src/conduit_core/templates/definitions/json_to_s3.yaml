# GENERATED BY: conduit template json_to_s3
# DESCRIPTION: Write JSON records to S3
# LAST UPDATED: 2025-01-18

version: "1.0"
project:
  name: "json_to_s3_pipeline"
  environment: "production"

sources:
  - name: "json_source"
    type: "json"
    path: "./data/input.json"        # UPDATE THIS
    encoding: "utf-8"

destinations:
  - name: "s3_dest"
    type: "s3"
    bucket: "my-output-bucket"        # UPDATE THIS
    path: "output/data.json"          # Object key in bucket

resources:
  - name: "json_to_s3"
    source: "json_source"
    destination: "s3_dest"
    query: ""
    mode: "append"

# HOW TO USE:
# 1. Update path, bucket, and object key fields
# 2. Provide AWS credentials via environment (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, etc.)
# 3. Save as `pipeline.yml`
# 4. Run: `conduit run pipeline.yml`
