# GENERATED BY: conduit template csv_to_postgresql
# DESCRIPTION: Ingest CSV into PostgreSQL
# LAST UPDATED: 2025-01-15

version: "1.0"
project:
  name: "csv_to_postgres_pipeline"  # UPDATE THIS
  environment: "production"

sources:
  - type: "csv"
    name: "csv_source"
    config:
      path: "./data/input.csv"      # UPDATE THIS
      delimiter: ","
      has_header: true

destinations:
  - type: "postgresql"
    name: "pg_dest"
    config:
      host: "localhost"             # UPDATE THIS
      port: 5432
      database: "target_db"         # UPDATE THIS
      user: "${PG_USER}"            # 🔒
      password: "${PG_PASSWORD}"    # 🔒
      table: "target_table"         # UPDATE THIS

pipelines:
  - name: "csv_to_postgres_pipeline"
    source: "csv_source"
    destination: "pg_dest"
    mode: "append"
    quality_checks:
      - column: "id"
        check: "not_null"
        action: "fail"

# HOW TO USE:
# 1. Update fields, export PG_USER/PG_PASSWORD.
# 2. Save as `pipeline.yml`.
# 3. Run: `conduit run pipeline.yml`.
