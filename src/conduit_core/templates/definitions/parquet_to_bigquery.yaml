# GENERATED BY: conduit template parquet_to_bigquery
# DESCRIPTION: Load Parquet files into BigQuery
# LAST UPDATED: 2025-01-15

version: "1.0"
project:
  name: "parquet_to_bq_pipeline"            # ⚠️ UPDATE THIS
  environment: "production"

sources:
  - type: "parquet"
    name: "parquet_source"
    config:
      path: "./data/input.parquet"          # ⚠️ UPDATE THIS

destinations:
  - type: "bigquery"
    name: "bq_dw"
    config:
      project: "${BQ_PROJECT_ID}"           # ⚠️ UPDATE THIS
      dataset: "TARGET_DATASET"             # ⚠️ UPDATE THIS
      table: "TARGET_TABLE"                 # ⚠️ UPDATE THIS
      credentials_json: "${BQ_SERVICE_ACCOUNT_JSON}"

pipelines:
  - name: "parquet_to_bq_pipeline"
    source: "parquet_source"
    destination: "bq_dw"
    mode: "append"

# 🚀 HOW TO USE:
# 1. Update all ⚠️ fields.
# 2. Set required environment variables for credentials.
# 3. Save as `pipeline.yml`.
# 4. Run: `conduit run pipeline.yml`.
