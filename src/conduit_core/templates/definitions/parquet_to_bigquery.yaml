# GENERATED BY: conduit template parquet_to_bigquery
# DESCRIPTION: Load Parquet files into BigQuery
# LAST UPDATED: 2025-01-18

version: "1.0"
project:
  name: "parquet_to_bigquery_pipeline"
  environment: "production"

sources:
  - name: "parquet_source"
    type: "parquet"
    path: "./data/input.parquet"    # UPDATE THIS

destinations:
  - name: "bigquery_dest"
    type: "bigquery"
    project: "${GCP_PROJECT_ID}"
    dataset: "analytics"              # UPDATE THIS
    table: "target_table"             # UPDATE THIS
    credentials_path: "${GOOGLE_APPLICATION_CREDENTIALS}"

resources:
  - name: "parquet_to_bigquery"
    source: "parquet_source"
    destination: "bigquery_dest"
    query: ""
    mode: "append"

# HOW TO USE:
# 1. Update all marked fields
# 2. Set GOOGLE_APPLICATION_CREDENTIALS
# 3. Save as `pipeline.yml`
# 4. Run: `conduit run pipeline.yml`
