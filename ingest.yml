sources:
  - name: my_azure_db
    type: azuresql

destinations:
  - name: print_to_terminal
    type: dummy_destination

resources:
  - name: get_real_users
    source: my_azure_db
    destination: print_to_terminal
    query: "SELECT UserID, Username, Email FROM dbo.Users;"