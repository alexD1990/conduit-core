# test_invalid_connections.yml
sources:
  - name: bad_csv
    type: csv
    path: /nonexistent/file.csv

destinations:
  - name: bad_postgres
    type: postgres
    host: invalid-host-12345.example.com
    port: 5432
    database: testdb
    user: testuser
    password: wrongpassword
    schema: public
    table: test_table

resources:
  - name: test
    source: bad_csv
    destination: bad_postgres
    query: "SELECT * FROM test"